package utils;

public class ReadingAnalytics {
    // Wind chill calculator
    // Wind Chill = 13.12 + 0.6215(temperature) - 11.37(windSpeed km/h ^ 0.16) + 0.3965(temperature)(windSpeed km/h ^ 0.16)
}